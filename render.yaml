services:
  - type: web
    name: dosificador-constructor-ia
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir web_app --bind 0.0.0.0:$PORT app:app
    autoDeploy: true
